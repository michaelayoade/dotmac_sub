apiVersion: 1

providers:
  - name: 'DotMac Dashboards'
    orgId: 1
    folder: 'DotMac'
    folderUid: dotmac
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
