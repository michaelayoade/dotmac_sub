# Runtime-readiness inventory baseline: LIVE local engine/session
# construction outside app.db's canonical SessionLocal factory, in
# production/operational surfaces (app/, scripts/, alembic/ — tests/ is
# tracked separately, see test_test_fixture_engine_family_is_swept).
#
# "Live" excludes historical residue — files already recorded in the
# repository's own frozen surface for a fully decommissioned integration.
# That exclusion is computed, not hand-applied here: see
# tests/architecture/session_construction_inventory.py's
# production_counts_by_file / historical_residue_paths for the mechanism
# and exactly which existing ledger it defers to, and
# docs/sub-runtime-readiness-inventory.md's "Historical / residual"
# section for why a decommissioned integration's one-time migration
# tooling imposes no DatabaseRuntime requirement. This file intentionally
# does not name those excluded paths — they are already named once, in the
# ledger this file's own scan mechanism defers to.
#
# Format: "<repo-relative path> <construction-site count>". A path's count
# only ever moves when the change that moved it explains why (new local
# construction the DatabaseRuntime seam must account for, or a site
# retired/migrated onto SessionLocal). See
# docs/sub-runtime-readiness-inventory.md for what each site is and why it
# exists.
alembic/env.py 1
app/db.py 2
app/services/auth_flow.py 1
app/services/db_session_adapter.py 1
app/services/events/dispatcher.py 1
app/services/events/handlers/owner_session.py 1
app/services/external_radius_targets.py 1
app/services/financial_imports.py 1
app/services/session_hooks.py 1
app/services/usage.py 1
app/tasks/_postgres_lock.py 1
scripts/billing/backfill_ledger_effective_date.py 1
scripts/ci/migrated_test_database.py 1
scripts/negotiated_price_offer_inventory.py 2
scripts/one_off/shadow_diff_committed_rate.py 1
