# Files that assign a credential's RADIUS profile columns
# (radius_profile_id, pre_throttle_radius_profile_id), counted as assignments.
#
# Ledger row COL-R5. `app/services/collections/**` is ABSENT from this file and
# must stay absent: collections requests a consequence, `account_lifecycle`
# permits or refuses it, and `radius_access_state` applies it. That is the
# whole point of the row, and `test_collections_writes_no_credential_profile`
# is what keeps it true.
#
# Everything listed here is OUT of the commercial retirement programme — each
# is a provisioning, catalog or session-enforcement decision, not a collections
# consequence. It is recorded rather than ignored so the ratchet's silence
# about these files is a stated position instead of an oversight. The owners
# are named in `app/services/collections_authority.py`.
#
# Two-directional and shrink-only. A rise fails. A fall ALSO fails unless the
# count is lowered here in the same change, because a guard that silently
# accepts a drop cannot tell real progress from a detector that stopped
# looking.
2 app/services/access_credential_binding.py
2 app/services/catalog/subscriptions.py
1 app/services/enforcement.py
1 app/services/pppoe_credentials.py
2 app/services/radius.py
5 app/services/radius_access_state.py
1 app/services/web_catalog_subscriptions.py
