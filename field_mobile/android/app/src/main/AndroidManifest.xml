<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Background location + a typed foreground service so "on shift" tracking
         keeps emitting fixes when the app is backgrounded or the screen is locked. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Hands-free voice job notes (speech_to_text). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <application
        android:label="dotmac_field"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Flutter's own deep-link handler routes an incoming VIEW
                 intent's path+query into the app's GoRouter, which hands the
                 exact callback path to OidcCallbackCoordinator. Without this
                 flag the intent filter below verifies but the link never
                 reaches Dart. -->
            <meta-data
                android:name="flutter_deeplinking_enabled"
                android:value="true" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- The VERIFIED redirect boundary for the OIDC ceremony (Android
                 App Links). android:autoVerify makes Android fetch
                 https://links.dotmac.io/.well-known/assetlinks.json at install
                 time and open this link in the app ONLY if that document names
                 this package and the certificate this build was actually signed
                 with. An unverified or wrongly-signed build gets the browser,
                 which is the point.

                 android:path is an EXACT match, deliberately not pathPrefix and
                 deliberately not pathPattern: a prefix would make every path
                 under /oidc/field/callback a legal place to deliver an
                 authorization code.

                 There is NO custom-scheme filter here and there must never be
                 one. A custom scheme is claimable by any app on the device, so
                 it delivers the code to whoever registered it first. See
                 lib/core/deeplink/oidc_redirect.dart. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="links.dotmac.io"
                    android:path="/oidc/field/callback"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required (Android 11+) for speech_to_text to discover the on-device
             speech recognition service. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
    </queries>
</manifest>
