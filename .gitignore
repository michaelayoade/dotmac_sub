# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
.env

# Node
node_modules/
npm-debug.log
yarn-error.log

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
*.bak-*
CLAUDE.md

# OS
.DS_Store
Thumbs.db

# Build artifacts
dist/
build/
*.egg-info/

# Database
*.db
*.sqlite3

# Logs
*.log
logs/

# Secrets
.env.bak.*
.env.local
.env.*.local
*.pem
*.key
secrets/
*_backup.json
*.bak.*

# Coverage
htmlcov/
.coverage
.pytest_cache/
.claude/
test-results/.last-run.json

# Migration runtime data
scripts/migration/.rebind_checkpoints/
/tmp/smartolt_*.json

# Sensitive credential exports
wifi_credentials.csv
SmartOLT_onus_list.csv
*_credentials.csv

# Reference materials & artifacts
screenshots/
uploads/
*.zip
mypy_errors.txt
scripts/export_wifi_credentials.py

# OLT CLI capture samples (auto-generated)
data/olt_samples/
.playwright-mcp/

# Runtime-uploaded avatars (keep the dir, ignore the uploads)
/static/avatars/*
!/static/avatars/.gitkeep

# Runtime-uploaded branding files (admin logo/favicon uploads, named <slug>_<12-hex>.<ext>;
# stock branding assets stay tracked)
/static/branding/**/*_[0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f].*

# Stray local screenshot capture at repo root
/login.png

# local type-check cache
.mypy_cache/

# Edge-case sweep generated artifacts (screenshots + json)
docs/testing/results/

# Terminal-IP backlog apply manifests (subscriber/assignment ids) — runtime artifacts
terminal_ip_*.json
# Post-cutover finance hand-off artifacts — reproducible, contain customer PII (regenerate from scripts/billing/)
post_cutover_*.csv
# Billing-violation finance decision packet — reproducible, customer PII (regenerate from scripts/billing/)
billing_violations_*.csv
# Billing dry-run snapshots (operational counts) — runtime artifacts
billing_dryrun_*.json
# Billing remediation manifests (invoice line ids, account refs) — runtime artifacts
remediation_*.json
