#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export HOME="${FLUTTER_HOME_DIR:-$ROOT_DIR/.flutter-home}"

mkdir -p "$HOME"
exec "$ROOT_DIR/.flutter/bin/flutter" "$@"
